# Production behavior lives in docker-compose.yml. Application images are supplied
# only by the mandatory digest-pinned release override.
services:
  ops-agent:
    environment:
      OPS_AGENT_COMPOSE_FILES: /workspace/docker-compose.yml,/workspace/docker-compose.prod.yml,/workspace/${RELEASE_COMPOSE}
